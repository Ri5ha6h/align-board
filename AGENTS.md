# Project Guidelines

- This project uses npm. Use npm commands for installing dependencies and running project scripts.
- Do not start the development server with `npm run dev`, `next dev`, or an equivalent command. The development server might already be running.
